# このリポジトリについて

熊崎式（五格剖象法）の姓名判断アプリ。`index.html` 単体で動き、GitHub Pages に置く。

## 落とし穴（ここに一番トークンを使う）

- **`index.html` は生成物。直接編集しない。**`build/template.html` の
  `/*__DATA__*/` `/*__CALC__*/` `/*__UI__*/` に `data.js` / `calc.js` / `ui.js` を
  注入して作られる。編集は `build/` 側で行い、`python3 build_app.py` で再生成する。
- **`build_app.py` は `data.js` が無いと止まる。**`data.js`・`kanji.json`・`ids.txt` は
  gitignore 済み。無い環境では `build/README.md` の手順で元データを curl してから
  `python3 build_data.py` を先に走らせる。
- **`calc.js` 末尾の `module.exports` 行は1行のまま保つ。**`build_app.py` が
  `if(typeof module!=='undefined'){...}` に一致する正規表現で1行削除している。
  複数行に折ると `index.html` に Node 用のコードが混入する。
- **鑑定文を触ったら `node test.js`。**`FORTUNE` は1数につき8項目
  （評価・数理名・解説・仕事・対人・心得・金運・健康）の配列、`FEMALE` は81数すべてに文字列。
  1つでも欠けると完全性テストが落ちる。金運・健康は後から足したため配列の末尾にあり、
  画面では 仕事→金運→対人→健康→心得 の順に出す（`ui.js` の `dts`）。
- **女性判定は据え置きが既定。**頭領数（21・23・33・39）と活動数（29）は
  `sex==='f'` のときだけ凶に転じる。未指定は評価を変えず注記だけ出す仕様なので、
  「未指定＝男性」に寄せないこと。
- **霊数は総格に加えない。**一字姓・一字名の霊数1は天格・地格・外格の計算にのみ足す。
- **画数は流派差がある。**数え方を変える提案をする前に `build/README.md` の
  「画数を修正したいとき」を読む。基準値テスト（`EXPECT`）が111字ぶん走っている。

## 判断に迷ったときの優先順位

1. 確実に動く・事実として正確
2. 後から直せる読みやすさ
3. 見た目の洗練

迷ったら上位を優先し、どう判断したかを最後に一行で報告する。

## 分量と範囲

- 応答は焦点を絞り、簡潔に。書き出す文書の長さは作業に必要な分量に合わせ、
  水増しの節・重複する要約・定型文で膨らませない。
- 頼まれたものを、頼まれた広さで。解釈が分かれると成果物が大きく変わるときだけ確認する。
  より良い方法があれば一文で述べ、作業は依頼どおり進める。
- サブエージェントへの委任は、独立していて並列化できる大きな作業だけ。
  自分の作業の検証・再確認には使わない。

## 検証

- 検証が必要なときは verify スキルを呼ぶ（常時は実行しない）。
